{
  "name": "@repo/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "NODE_ENV=development tsup --watch --onSuccess \"NODE_ENV=development electron .\"",
    "build": "tsup",
    "build:web": "pnpm --filter @repo/web build",
    "package": "pnpm build:web && pnpm build && electron-builder --config electron-builder.json",
    "package:mac": "pnpm build:web && pnpm build && electron-builder --mac --config electron-builder.json",
    "package:win": "pnpm build:web && pnpm build && electron-builder --win --config electron-builder.json",
    "package:linux": "pnpm build:web && pnpm build && electron-builder --linux --config electron-builder.json",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "electron-log": "^5.4.3",
    "electron-updater": "^6.7.3"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^22.10.7",
    "electron": "^34.0.2",
    "electron-builder": "^25.1.8",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
